1.（多函数形式）假设已经有一个排序好的数组，要求输入一个数，判断这个数在该数组中是否存在，
  如果存在将该数删除后输出，
  如果没有，将该数插入到该数组中，插入后数组仍是排序好的。然后输出

2.（多函数形式）有某项体育运动的评分规则如下：10个评委给出10个分数，去掉一个最低分，去掉一个最高分，剩余8个分值的均值为选手的最终得分。
  写程序协助赛事组织计算选手得分

3.（多函数形式）已知有大小为10的字符数组(自行初始化)，去掉用户输入的指定字符（有多个时全部删除）后输出。

4. 从键盘输入一批正数存入一维数组中（以-1为结束标记），求有效数的和与平均值并输出。（多函数形式）

5. 从键盘输入一组数据存入数组a[4][4]中，求其四周元素之和（不要求多函数形式）

6. 定义一个字符数组用字符串常量初始化，查找指定字符第一次出现的下标位置（多函数形式）

7. 定义一个字符数组（str1）用字符串常量初始化，再定义一个同样大小的字符数组（str2），将str1中的有效字符拷贝到str2中（多函数形式）

8. 定义两个个字符数组（str1、str2）用字符串常量初始化，再定义一个字符数组（str3），str3的大小为str1的大小加str2的大小，
   将str1和str2中的字符串连接成一个字符串存放到str3中，如："abc","123" ----> "abc123"（多函数形式）


提交方式：源码打包为1224hw_姓名.xxx发送在本群
截止时间：12月26日22:00前