分别用地址法 和 指针法改写以下作业

1.（多函数形式）已知有大小为10的字符数组(自行初始化)，去掉用户输入的指定字符（有多个时全部删除）后输出。

2. 从键盘输入一批正数存入一维数组中（以-1为结束标记），求有效数的和与平均值并输出。（多函数形式）

3. 从键盘输入一组数据存入数组a[4][4]中，求其四周元素之和（不要求多函数形式）

4. 定义一个字符数组用字符串常量初始化，查找指定字符第一次出现的下标位置（多函数形式）

5. 定义一个字符数组（str1）用字符串常量初始化，再定义一个同样大小的字符数组（str2），将str1中的有效字符拷贝到str2中（多函数形式）

6. 定义两个个字符数组（str1、str2）用字符串常量初始化，再定义一个字符数组（str3），str3的大小为str1的大小加str2的大小，
   将str1和str2中的字符串连接成一个字符串存放到str3中，如："abc","123" ----> "abc123"（多函数形式）


提交方式：源码打包为1227hw_姓名.xxx发送在本群
截止时间：12月27日22:00前
